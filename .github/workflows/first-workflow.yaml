name: First Workflow
on: [push]

jobs: 
    run-shell-commands: 
        runs-on: ubuntu-lastest
        steps: 
            - name: echo a string
              run: echo "Hello world"
            - name: Multiline Command
              run: |
                node -v
                npm -v